/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ! This sass options is not working
  // sassOptions: {
  //   includePaths: [path.join(__dirname, "styles")],
  // },
};

module.exports = nextConfig;
